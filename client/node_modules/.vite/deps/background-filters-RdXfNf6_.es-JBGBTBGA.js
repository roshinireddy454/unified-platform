import {
  VirtualBackground,
  createRenderer,
  isMediaPipePlatformSupported,
  isPlatformSupported,
  loadMediaPipe,
  loadTFLite
} from "./chunk-KWPR67KY.js";
import {
  clsx_default,
  disposeOfMediaStream,
  useCall,
  useCallStateHooks
} from "./chunk-GIZTFSNM.js";
import "./chunk-XRXMA762.js";
import {
  require_react_dom
} from "./chunk-KDCVS43I.js";
import {
  require_jsx_runtime
} from "./chunk-S725DACQ.js";
import "./chunk-5SMBE6KA.js";
import {
  require_react
} from "./chunk-RLJ2RCJQ.js";
import {
  __toESM
} from "./chunk-DC5AMYBS.js";

// node_modules/@stream-io/video-react-sdk/dist/background-filters-RdXfNf6_.es.js
var import_jsx_runtime = __toESM(require_jsx_runtime());
var import_react = __toESM(require_react());
var import_react_dom = __toESM(require_react_dom());
var ALPHA = 0.2;
var DEFAULT_FPS = 30;
var DEVIATION_LIMIT = 0.5;
var OUTLIER_PERSISTENCE = 5;
var FilterEngine;
(function(FilterEngine2) {
  FilterEngine2[FilterEngine2["TF"] = 0] = "TF";
  FilterEngine2[FilterEngine2["MEDIA_PIPE"] = 1] = "MEDIA_PIPE";
  FilterEngine2[FilterEngine2["NONE"] = 2] = "NONE";
})(FilterEngine || (FilterEngine = {}));
var determineEngine = async (useLegacyFilter, forceSafariSupport, forceMobileSupport) => {
  if (useLegacyFilter) {
    const isTfPlatformSupported = await isPlatformSupported({
      forceSafariSupport,
      forceMobileSupport
    });
    return isTfPlatformSupported ? FilterEngine.TF : FilterEngine.NONE;
  }
  const isMediaPipeSupported = await isMediaPipePlatformSupported({
    forceSafariSupport,
    forceMobileSupport
  });
  return isMediaPipeSupported ? FilterEngine.MEDIA_PIPE : FilterEngine.NONE;
};
var BackgroundFiltersProvider = (props) => {
  var _a, _b, _c;
  const { ContextProvider, children, backgroundImages = [], backgroundFilter: bgFilterFromProps = void 0, backgroundImage: bgImageFromProps = void 0, backgroundBlurLevel: bgBlurLevelFromProps = void 0, tfFilePath, modelFilePath, useLegacyFilter, basePath, onError, performanceThresholds, forceSafariSupport, forceMobileSupport } = props;
  const call = useCall();
  const { useCallStatsReport } = useCallStateHooks();
  const callStatsReport = useCallStatsReport();
  const [backgroundFilter, setBackgroundFilter] = (0, import_react.useState)(bgFilterFromProps);
  const [backgroundImage, setBackgroundImage] = (0, import_react.useState)(bgImageFromProps);
  const [backgroundBlurLevel, setBackgroundBlurLevel] = (0, import_react.useState)(bgBlurLevelFromProps);
  const [showLowFpsWarning, setShowLowFpsWarning] = (0, import_react.useState)(false);
  const [isLoading, setIsLoading] = (0, import_react.useState)(false);
  const cameraFrameRate = (_b = (_a = callStatsReport == null ? void 0 : callStatsReport.publisherStats) == null ? void 0 : _a.camera) == null ? void 0 : _b.frameRate;
  const sourceFps = (performanceThresholds == null ? void 0 : performanceThresholds.defaultFps) || cameraFrameRate || DEFAULT_FPS;
  const fpsWarningThresholdLower = (performanceThresholds == null ? void 0 : performanceThresholds.fpsWarningThresholdLower) ?? sourceFps * 0.75;
  const fpsWarningThresholdUpper = (performanceThresholds == null ? void 0 : performanceThresholds.fpsWarningThresholdUpper) ?? sourceFps * 0.85;
  const emaRef = (0, import_react.useRef)(sourceFps);
  const outlierStreakRef = (0, import_react.useRef)(0);
  const handleStats = (0, import_react.useCallback)((stats) => {
    const fps = stats == null ? void 0 : stats.fps;
    if (fps === void 0 || fps === null) {
      emaRef.current = sourceFps;
      outlierStreakRef.current = 0;
      setShowLowFpsWarning(false);
      return;
    }
    const prevEma = emaRef.current;
    const deviation = Math.abs(fps - prevEma) / prevEma;
    const isOutlier = fps < prevEma && deviation > DEVIATION_LIMIT;
    outlierStreakRef.current = isOutlier ? outlierStreakRef.current + 1 : 0;
    if (isOutlier && outlierStreakRef.current < OUTLIER_PERSISTENCE)
      return;
    emaRef.current = ALPHA * fps + (1 - ALPHA) * prevEma;
    setShowLowFpsWarning((prev) => {
      if (prev && emaRef.current > fpsWarningThresholdUpper)
        return false;
      if (!prev && emaRef.current < fpsWarningThresholdLower)
        return true;
      return prev;
    });
  }, [fpsWarningThresholdLower, fpsWarningThresholdUpper, sourceFps]);
  const performance = (0, import_react.useMemo)(() => {
    var _a2;
    if (!backgroundFilter) {
      return { degraded: false };
    }
    const reasons = [];
    if (showLowFpsWarning) {
      reasons.push("frame-drop");
    }
    const qualityLimitationReasons = (_a2 = callStatsReport == null ? void 0 : callStatsReport.publisherStats) == null ? void 0 : _a2.qualityLimitationReasons;
    if (showLowFpsWarning && qualityLimitationReasons && (qualityLimitationReasons == null ? void 0 : qualityLimitationReasons.includes("cpu"))) {
      reasons.push("cpu-throttling");
    }
    return {
      degraded: reasons.length > 0,
      reason: reasons.length > 0 ? reasons : void 0
    };
  }, [
    showLowFpsWarning,
    (_c = callStatsReport == null ? void 0 : callStatsReport.publisherStats) == null ? void 0 : _c.qualityLimitationReasons,
    backgroundFilter
  ]);
  const prevDegradedRef = (0, import_react.useRef)(void 0);
  (0, import_react.useEffect)(() => {
    const currentDegraded = performance.degraded;
    const prevDegraded = prevDegradedRef.current;
    if (!!backgroundFilter && prevDegraded !== void 0 && prevDegraded !== currentDegraded) {
      call == null ? void 0 : call.tracer.trace("backgroundFilters.performance", {
        degraded: currentDegraded,
        reason: performance == null ? void 0 : performance.reason,
        fps: emaRef.current
      });
    }
    prevDegradedRef.current = currentDegraded;
  }, [
    performanceThresholds,
    performance.degraded,
    performance.reason,
    backgroundFilter,
    call == null ? void 0 : call.tracer
  ]);
  const applyBackgroundImageFilter = (0, import_react.useCallback)((imageUrl) => {
    setBackgroundFilter("image");
    setBackgroundImage(imageUrl);
  }, []);
  const applyBackgroundBlurFilter = (0, import_react.useCallback)((blurLevel = "high") => {
    setBackgroundFilter("blur");
    setBackgroundBlurLevel(blurLevel);
  }, []);
  const disableBackgroundFilter = (0, import_react.useCallback)(() => {
    setBackgroundFilter(void 0);
    setBackgroundImage(void 0);
    setBackgroundBlurLevel(void 0);
    emaRef.current = sourceFps;
    outlierStreakRef.current = 0;
    setShowLowFpsWarning(false);
  }, [sourceFps]);
  const [engine, setEngine] = (0, import_react.useState)(FilterEngine.NONE);
  const [isSupported, setIsSupported] = (0, import_react.useState)(false);
  (0, import_react.useEffect)(() => {
    determineEngine(useLegacyFilter, forceSafariSupport, forceMobileSupport).then((determinedEngine) => {
      setEngine(determinedEngine);
      setIsSupported(determinedEngine !== FilterEngine.NONE);
    });
  }, [forceMobileSupport, forceSafariSupport, useLegacyFilter]);
  const [tfLite, setTfLite] = (0, import_react.useState)();
  (0, import_react.useEffect)(() => {
    if (engine !== FilterEngine.TF)
      return;
    loadTFLite({ basePath, modelFilePath, tfFilePath }).then(setTfLite).catch((err) => console.error("Failed to load TFLite", err));
  }, [basePath, engine, modelFilePath, tfFilePath]);
  const [mediaPipe, setMediaPipe] = (0, import_react.useState)();
  (0, import_react.useEffect)(() => {
    if (engine !== FilterEngine.MEDIA_PIPE)
      return;
    loadMediaPipe({
      basePath,
      modelPath: modelFilePath
    }).then(setMediaPipe).catch((err) => console.error("Failed to preload MediaPipe", err));
  }, [engine, modelFilePath, basePath]);
  const handleError = (0, import_react.useCallback)((error) => {
    console.warn("[filters] Filter encountered an error and will be disabled");
    disableBackgroundFilter();
    onError == null ? void 0 : onError(error);
  }, [disableBackgroundFilter, onError]);
  const isReady = useLegacyFilter ? !!tfLite : !!mediaPipe;
  const contextValue = {
    isSupported,
    performance,
    isReady,
    isLoading,
    backgroundImage,
    backgroundBlurLevel,
    backgroundFilter,
    disableBackgroundFilter,
    applyBackgroundBlurFilter,
    applyBackgroundImageFilter,
    backgroundImages,
    tfFilePath,
    modelFilePath,
    basePath,
    onError: handleError
  };
  return (0, import_jsx_runtime.jsxs)(ContextProvider.Provider, { value: contextValue, children: [children, isReady && (0, import_jsx_runtime.jsx)(BackgroundFilters, { api: contextValue, tfLite, engine, onStats: handleStats, setIsLoading })] });
};
var BackgroundFilters = (props) => {
  const call = useCall();
  const { engine, api, tfLite, onStats, setIsLoading } = props;
  const { children, start } = useRenderer(api, tfLite, call, engine);
  const { onError, backgroundFilter } = api;
  const handleErrorRef = (0, import_react.useRef)(void 0);
  handleErrorRef.current = onError;
  const handleStatsRef = (0, import_react.useRef)(void 0);
  handleStatsRef.current = onStats;
  (0, import_react.useEffect)(() => {
    if (!call || !backgroundFilter)
      return;
    setIsLoading(true);
    const { unregister, registered } = call.camera.registerFilter((ms) => {
      return start(ms, (error) => {
        var _a;
        return (_a = handleErrorRef.current) == null ? void 0 : _a.call(handleErrorRef, error);
      }, (stats) => {
        var _a;
        return (_a = handleStatsRef.current) == null ? void 0 : _a.call(handleStatsRef, stats);
      });
    });
    registered.finally(() => {
      setIsLoading(false);
    });
    return () => {
      unregister().catch((err) => console.warn(`Can't unregister filter`, err));
    };
  }, [call, start, backgroundFilter, setIsLoading]);
  return children;
};
var useRenderer = (api, tfLite, call, engine) => {
  const { backgroundFilter, backgroundBlurLevel, backgroundImage, modelFilePath, basePath } = api;
  const videoRef = (0, import_react.useRef)(null);
  const canvasRef = (0, import_react.useRef)(null);
  const bgImageRef = (0, import_react.useRef)(null);
  const [videoSize, setVideoSize] = (0, import_react.useState)({
    width: 1920,
    height: 1080
  });
  const start = (0, import_react.useCallback)((ms, onError, onStats) => {
    let outputStream;
    let processor;
    let renderer;
    const output = new Promise((resolve, reject) => {
      if (!backgroundFilter) {
        reject(new Error("No filter specified"));
        return;
      }
      const videoEl = videoRef.current;
      const canvasEl = canvasRef.current;
      const bgImageEl = bgImageRef.current;
      const [track] = ms.getVideoTracks();
      if (!track) {
        reject(new Error("No video tracks in input media stream"));
        return;
      }
      if (engine === FilterEngine.MEDIA_PIPE) {
        call == null ? void 0 : call.tracer.trace("backgroundFilters.enable", {
          backgroundFilter,
          backgroundBlurLevel,
          backgroundImage,
          engine
        });
        if (!videoEl) {
          reject(new Error("Renderer started before elements are ready"));
          return;
        }
        const trackSettings = track.getSettings();
        (0, import_react_dom.flushSync)(() => setVideoSize({
          width: trackSettings.width ?? 0,
          height: trackSettings.height ?? 0
        }));
        processor = new VirtualBackground(track, {
          basePath,
          modelPath: modelFilePath,
          backgroundBlurLevel,
          backgroundImage,
          backgroundFilter
        }, { onError, onStats });
        processor.start().then((processedTrack) => {
          outputStream = new MediaStream([processedTrack]);
          resolve(outputStream);
        }).catch((error) => {
          reject(error);
        });
        return;
      }
      if (engine === FilterEngine.TF) {
        if (!videoEl || !canvasEl || backgroundImage && !bgImageEl) {
          reject(new Error("Renderer started before elements are ready"));
          return;
        }
        videoEl.srcObject = ms;
        videoEl.play().then(() => {
          const trackSettings = track.getSettings();
          (0, import_react_dom.flushSync)(() => setVideoSize({
            width: trackSettings.width ?? 0,
            height: trackSettings.height ?? 0
          }));
          call == null ? void 0 : call.tracer.trace("backgroundFilters.enable", {
            backgroundFilter,
            backgroundBlurLevel,
            backgroundImage,
            engine
          });
          if (!tfLite) {
            reject(new Error("TensorFlow Lite not loaded"));
            return;
          }
          renderer = createRenderer(tfLite, videoEl, canvasEl, {
            backgroundFilter,
            backgroundBlurLevel,
            backgroundImage: bgImageEl ?? void 0
          }, onError);
          outputStream = canvasEl.captureStream();
          resolve(outputStream);
        }, () => {
          reject(new Error("Could not play the source video stream"));
        });
        return;
      }
      reject(new Error("No supported engine available"));
    });
    return {
      output,
      stop: () => {
        call == null ? void 0 : call.tracer.trace("backgroundFilters.disable", null);
        processor == null ? void 0 : processor.stop();
        renderer == null ? void 0 : renderer.dispose();
        if (videoRef.current)
          videoRef.current.srcObject = null;
        if (outputStream)
          disposeOfMediaStream(outputStream);
      }
    };
  }, [
    backgroundBlurLevel,
    backgroundFilter,
    backgroundImage,
    call == null ? void 0 : call.tracer,
    tfLite,
    engine,
    modelFilePath,
    basePath
  ]);
  const children = (0, import_jsx_runtime.jsxs)("div", { className: "str-video__background-filters", children: [(0, import_jsx_runtime.jsx)("video", { className: clsx_default("str-video__background-filters__video", videoSize.height > videoSize.width && "str-video__background-filters__video--tall"), ref: videoRef, playsInline: true, muted: true, controls: false, ...videoSize }), backgroundImage && (0, import_jsx_runtime.jsx)("img", { className: "str-video__background-filters__background-image", alt: "Background", ref: bgImageRef, crossOrigin: "anonymous", src: backgroundImage, ...videoSize }), (0, import_jsx_runtime.jsx)("canvas", { className: "str-video__background-filters__target-canvas", ...videoSize, ref: canvasRef })] });
  return { start, children };
};
export {
  BackgroundFiltersProvider
};
//# sourceMappingURL=background-filters-RdXfNf6_.es-JBGBTBGA.js.map
